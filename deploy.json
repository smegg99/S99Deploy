{
	"name": "s99deploy-site",
	"build": [
		"go build -trimpath -o bin/s99deploy .",
		"go build -trimpath -o bin/s99deploy-site ./site"
	],
	"run": ["bin/s99deploy-site"],
	"env": { "CONFIG_PATH": "config.json" },
	"check": { "port": 9250 }
}
